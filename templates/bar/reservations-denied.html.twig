<ul class="list-group">
    {% for d in reservations.denied %}
        <li class="list-group-item">
            <div class="float-md-left">
                {{ d.name }} : pour <b>{{ d.nb_persons }} personnes</b> le <b>{{ d.date|date("d/m/Y à h:i") }}</b>
                <br><small>{{ d.phone }} - {{ d.email }}</small>
            </div>
            <div class="float-md-right">
                {% include 'components/btn-modal-reservation-accept.html.twig' %}
            </div>
        </li>
    {% endfor %}
</ul>
